[project]
name = "distributed-video-ai-platform"
version = "0.1.0"
description = "Large-Scale Video Analytics Architecture"
requires-python = ">=3.10"
dependencies = ["kafka-python==2.0.2", "pyyaml>=6.0"]

[project.optional-dependencies]
test = ["pytest==8.4.2"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
